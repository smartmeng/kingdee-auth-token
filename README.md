# kingdee-auth-token

```js
const getKingdeeAuthToken = require('kingdee-auth-token')
const appKey = ''
const appSecret = ''
const clientID = ''
const clientSecret = ''
let result = await getKingdeeAuthToken({ appKey, appSecret, clientID, clientSecret })
console.log(result)
```
